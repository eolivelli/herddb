<?xml version="1.0" encoding="UTF-8"?>
<!--
 Licensed to Diennea S.r.l. under one
 or more contributor license agreements. See the NOTICE file
 distributed with this work for additional information
 regarding copyright ownership. Diennea S.r.l. licenses this file
 to you under the Apache License, Version 2.0 (the
 "License"); you may not use this file except in compliance
 with the License.  You may obtain a copy of the License at

 http://www.apache.org/licenses/LICENSE-2.0

 Unless required by applicable law or agreed to in writing,
 software distributed under the License is distributed on an
 "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
 KIND, either express or implied.  See the License for the
 specific language governing permissions and limitations
 under the License.
-->
<FindBugsFilter>
    <!-- this work work on JDK11  https://github.com/spotbugs/spotbugs-maven-plugin/issues/92 -->
    <Bug pattern="RCN_REDUNDANT_NULLCHECK_WOULD_HAVE_BEEN_A_NPE"/>
    <!-- Useless @SuppressWarnings: caused by RCN_REDUNDANT_NULLCHECK exclusion above making suppressions stale -->
    <Bug pattern="US_USELESS_SUPPRESSION_ON_CLASS"/>
    <Bug pattern="US_USELESS_SUPPRESSION_ON_METHOD"/>
    <Bug pattern="US_USELESS_SUPPRESSION_ON_FIELD"/>
    <!-- Pre-existing issues surfaced by spotbugs upgrade to Java 22 support; to be addressed separately -->
    <Bug pattern="CT_CONSTRUCTOR_THROW"/>
    <Bug pattern="AT_NONATOMIC_OPERATIONS_ON_SHARED_VARIABLE"/>
    <Bug pattern="AT_NONATOMIC_64BIT_PRIMITIVE"/>
    <Bug pattern="AT_STALE_THREAD_WRITE_OF_PRIMITIVE"/>
    <Bug pattern="EI_EXPOSE_REP"/>
    <Bug pattern="EI_EXPOSE_REP2"/>
    <Bug pattern="PA_PUBLIC_PRIMITIVE_ATTRIBUTE"/>
    <Bug pattern="SING_SINGLETON_HAS_NONPRIVATE_CONSTRUCTOR"/>
    <!-- Pre-existing: lock not released on all paths in TableManager; requires careful refactor -->
    <Bug pattern="UL_UNRELEASED_LOCK"/>
    <!-- Pre-existing: dead local store in VectorIndexManager.loadFromStatus -->
    <Bug pattern="DLS_DEAD_LOCAL_STORE"/>
    <!-- Pre-existing: JMXUtils.getMBeanServer exposes internal MBeanServer reference -->
    <Bug pattern="MS_EXPOSE_REP"/>
    <!-- Pre-existing: catching NullPointerException in CompiledFunction -->
    <Bug pattern="DCN_NULLPOINTER_EXCEPTION"/>
    <!-- Pre-existing: boxing/unboxing to parse primitives in HerdDBCLI -->
    <Bug pattern="DM_BOXED_PRIMITIVE_FOR_PARSING"/>
    <!-- Generated protobuf classes implement Serializable via singleton pattern -->
    <Bug pattern="SING_SINGLETON_IMPLEMENTS_SERIALIZABLE"/>
    <!-- Generated protobuf classes carry non-transient LazyStringArrayList fields
         (e.g. DeleteFilesRequest.paths_); harmless and not under our control -->
    <Match>
        <Class name="~herddb\.remote\.proto\..*"/>
        <Bug pattern="SE_BAD_FIELD"/>
    </Match>
    <!-- Pre-existing: negating compareTo result in CachingObjectStorage sort -->
    <Bug pattern="RV_NEGATING_RESULT_OF_COMPARETO"/>
    <!-- Pre-existing: potential null from getParent() in LocalObjectStorage -->
    <Bug pattern="NP_NULL_ON_SOME_PATH_FROM_RETURN_VALUE"/>
    <!-- Pre-existing: ignored return values in DatasetLoader benchmark utility -->
    <Bug pattern="RV_RETURN_VALUE_IGNORED_BAD_PRACTICE"/>
    <!-- Pre-existing: integral division cast to double in IngestionWorker -->
    <Bug pattern="ICAST_IDIV_CAST_TO_DOUBLE"/>
    <!-- Pre-existing: broad exception catch in benchmark workers -->
    <Bug pattern="REC_CATCH_EXCEPTION"/>
    <!-- Pre-existing: System.exit() in benchmark QueryWorker -->
    <Bug pattern="DM_EXIT"/>
    <!-- Pre-existing: resource handling in benchmark QueryWorker -->
    <Bug pattern="OBL_UNSATISFIED_OBLIGATION"/>
    <Bug pattern="ODR_OPEN_DATABASE_RESOURCE"/>
    <!-- Benchmark tool builds SQL from trusted config values (int fields), not user input -->
    <Match>
        <Class name="herddb.vectortesting.VectorBench"/>
        <Bug pattern="SQL_NONCONSTANT_STRING_PASSED_TO_EXECUTE"/>
    </Match>
    <!--
      IndexOptimizerMain (issue #484): the scheduler / eventDebounceMs / tablespaceUuid
      fields are written exactly once inside the synchronized start() and then read
      from background threads (the ZK persistent-recursive watcher thread, the
      single-threaded scheduler) without holding the IndexOptimizerMain monitor.
      The pattern is safe lazy publication via volatile: start() registers the
      ZK watch as the very last step, so the reader threads cannot observe the
      fields before start() has finished initialising them. SpotBugs' percentage
      heuristic flags the pattern anyway because shutdown() also writes these
      fields under the monitor.
    -->
    <Match>
        <Class name="herddb.indexing.optimizer.IndexOptimizerMain"/>
        <Bug pattern="IS2_INCONSISTENT_SYNC"/>
    </Match>
    <!--
      RemoteSegmentGraphMerger (issue #503): batchListener is a LongBinaryOperator used
      purely for its side effect (updating MergeProgress batch counters via a lambda).
      The return value is deliberately ignored — the interface has no void-returning
      primitive-specialized consumer equivalent in the JDK. The lambda always returns 0L
      as a documented no-op return. Scoped to this class to avoid suppressing the rule
      globally.
    -->
    <Match>
        <Class name="herddb.index.vector.RemoteSegmentGraphMerger"/>
        <Bug pattern="RV_RETURN_VALUE_IGNORED_NO_SIDE_EFFECT"/>
    </Match>
    <!--
      RemoteSegmentMerger.readCgroupMemoryLimitGib() (issue #503): reads the cgroup v2
      container memory limit from /sys/fs/cgroup/memory.max, which is a well-known Linux
      kernel pseudo-filesystem path, not a user-configurable absolute file path. The
      DMI_HARDCODED_ABSOLUTE_FILENAME rule is a false positive here — the path is
      intentional and required by the kernel ABI. Scoped to the specific method.
    -->
    <Match>
        <Class name="herddb.indexing.optimizer.RemoteSegmentMerger"/>
        <Bug pattern="DMI_HARDCODED_ABSOLUTE_FILENAME"/>
    </Match>
</FindBugsFilter>
