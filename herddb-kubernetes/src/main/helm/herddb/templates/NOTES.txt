HerdDB has been deployed.

Server (JDBC endpoint):
  {{ include "herddb.fullname" . }}-server-0.{{ include "herddb.fullname" . }}-server.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.server.port }}

JDBC URL:
  {{ include "herddb.jdbcUrl" . }}

File servers ({{ .Values.fileServer.replicaCount }} replicas):
  {{ include "herddb.remoteFileServers" . }}

{{- if .Values.tools.enabled }}

Tools pod — run the CLI:
  kubectl exec -it -n {{ .Release.Namespace }} \
    deploy/{{ include "herddb.fullname" . }}-tools -- herddb-cli

  Or for a one-off query:
  kubectl exec -n {{ .Release.Namespace }} \
    deploy/{{ include "herddb.fullname" . }}-tools -- \
    herddb-cli -q "SELECT * FROM SYSTABLES"
{{- end }}
{{- if .Values.prometheus.enabled }}

Prometheus:
  kubectl port-forward -n {{ .Release.Namespace }} \
    svc/{{ include "herddb.fullname" . }}-prometheus 9090:9090
{{- end }}
{{- if .Values.grafana.enabled }}

Grafana:
  kubectl port-forward -n {{ .Release.Namespace }} \
    svc/{{ include "herddb.fullname" . }}-grafana 3000:3000
  Default credentials: admin / admin
{{- end }}
