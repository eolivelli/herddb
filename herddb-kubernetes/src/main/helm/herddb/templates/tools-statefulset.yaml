{{- if .Values.tools.enabled -}}
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "herddb.fullname" . }}-tools
  labels:
    {{- include "herddb.labels" . | nindent 4 }}
    app.kubernetes.io/component: tools
spec:
  serviceName: {{ include "herddb.fullname" . }}-tools
  replicas: 1
  persistentVolumeClaimRetentionPolicy:
    whenDeleted: Retain
    whenScaled: Retain
  selector:
    matchLabels:
      {{- include "herddb.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: tools
  template:
    metadata:
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/tools-configmap.yaml") . | sha256sum }}
      labels:
        {{- include "herddb.labels" . | nindent 8 }}
        app.kubernetes.io/component: tools
    spec:
      serviceAccountName: {{ include "herddb.serviceAccountName" . }}
      {{- with .Values.image.pullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tools.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tools.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tools.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: tools
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command: ["sleep"]
          args: ["infinity"]
          env:
            - name: HERDDB_JDBC_URL
              value: {{ include "herddb.jdbcUrl" . | quote }}
            - name: HERDDB_INDEXING_ZK
              value: {{ include "herddb.zkAddress" . | quote }}
            - name: HERDDB_FILESERVER_SERVER
              value: {{ include "herddb.fileServerAdminAddress" . | quote }}
            - name: JAVA_OPTS
              value: {{ .Values.tools.javaOpts | default "-Xms128m -Xmx256m -Djava.net.preferIPv4Stack=true" | quote }}
            - name: VECTORBENCH_DATASET_DIR
              value: /opt/herddb/vector-datasets
            - name: VECTORBENCH_ADMIN_PORT
              value: {{ .Values.tools.adminPort | default "8080" | quote }}
            {{- if .Values.tools.gcs.datasetsBucket }}
            - name: VECTORBENCH_DATASETS_BUCKET
              value: "gs://{{ .Values.tools.gcs.datasetsBucket }}"
            {{- end }}
            {{- if .Values.tools.gcs.credentialsSecret }}
            - name: GCS_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.tools.gcs.credentialsSecret }}
                  key: S3_ACCESS_KEY
            - name: GCS_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.tools.gcs.credentialsSecret }}
                  key: S3_SECRET_KEY
            {{- end }}
          resources:
            {{- toYaml .Values.tools.resources | nindent 12 }}
          volumeMounts:
            - name: tools-scripts
              mountPath: /usr/local/bin/herddb-cli
              subPath: herddb-cli
            - name: tools-scripts
              mountPath: /usr/local/bin/indexing-admin
              subPath: indexing-admin
            - name: tools-scripts
              mountPath: /usr/local/bin/fileserver-admin
              subPath: fileserver-admin
            - name: datasets
              mountPath: /opt/herddb/vector-datasets
      volumes:
        - name: tools-scripts
          configMap:
            name: {{ include "herddb.fullname" . }}-tools-scripts
            defaultMode: 0755
        {{- if .Values.tools.storage.datasets.hostPath }}
        - name: datasets
          hostPath:
            path: {{ .Values.tools.storage.datasets.hostPath | quote }}
            type: DirectoryOrCreate
        {{- end }}
  {{- if not .Values.tools.storage.datasets.hostPath }}
  volumeClaimTemplates:
    - metadata:
        name: datasets
      spec:
        accessModes: ["ReadWriteOnce"]
        {{- if .Values.tools.storage.datasets.storageClass }}
        storageClassName: {{ .Values.tools.storage.datasets.storageClass | quote }}
        {{- end }}
        resources:
          requests:
            storage: {{ .Values.tools.storage.datasets.size }}
  {{- end }}
{{- end }}
